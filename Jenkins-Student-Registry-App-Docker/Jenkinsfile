pipeline {
    agent {
        docker {
            image 'node:18'
            args '-u root'
            reuseNode true
        }
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage("Install dependencies") {
            steps {
                dir('exercise') {
                    sh 'npm install'
                }
            }
        }
        stage("Run tests") {
            steps {
                dir('exercise') {
                    sh 'npm test'
                }
            }
        }
    }
}