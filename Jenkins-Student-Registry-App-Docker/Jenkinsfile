pipeline {
    agent {
        docker {
            image 'node:18'
            args '-u root'
            reuseNode true
        }
    }

    stages {
        stage("Install dependencies") {
            steps {
                sh "npm install"
            }
        }
        stage("Run tests") {
            steps {
                sh "npm test"
            }
        }
    }
}